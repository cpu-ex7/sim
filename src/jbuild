(jbuild_version 1)

(ocamllex (lexer labelLexer))
(menhir ((modules (parser labelParser))))

(library
 ((name           Simulator)
  (flags          (-ccopt -O3 -cclib -O3 -w +a-4))
  (ocamlopt_flags (-inline 100))))
